---
---

<section
  class="coaching-hero-section relative min-h-screen flex items-center overflow-visible"
>
  <!-- Decorative wavy borders -->
  <div class="absolute top-0 left-0 right-0 h-20 overflow-hidden">
    <svg
      class="absolute top-0 w-full h-full"
      viewBox="0 0 1440 100"
      preserveAspectRatio="none"
    >
      <path
        d="M0,20 C200,60 400,10 600,40 C800,70 1000,20 1200,50 C1300,60 1400,40 1440,45 L1440,0 L0,0 Z"
        fill="#ef4444"
        stroke="#dc2626"
        stroke-width="3"></path>
    </svg>
  </div>

  <div class="absolute bottom-0 left-0 right-0 h-20 overflow-hidden">
    <svg
      class="absolute bottom-0 w-full h-full"
      viewBox="0 0 1440 100"
      preserveAspectRatio="none"
    >
      <path
        d="M0,55 C200,30 400,80 600,60 C800,30 1000,70 1200,50 C1300,40 1400,60 1440,55 L1440,100 L0,100 Z"
        fill="#ef4444"
        stroke="#dc2626"
        stroke-width="3"></path>
    </svg>
  </div>

  <!-- Gradient background with animated blobs -->
  <div class="absolute inset-0">
    <div
      class="absolute inset-0 bg-gradient-to-br from-blue-50/50 via-purple-50/30 to-pink-50/50"
    >
    </div>

    <!-- Animated gradient orbs -->
    <div
      class="absolute top-0 -left-40 w-96 h-96 bg-gradient-to-br from-blue-200/30 to-indigo-200/30 rounded-full blur-3xl animate-blob"
    >
    </div>
    <div
      class="absolute top-40 -right-40 w-96 h-96 bg-gradient-to-br from-purple-200/30 to-pink-200/30 rounded-full blur-3xl animate-blob animation-delay-2000"
    >
    </div>
    <div
      class="absolute -bottom-40 left-1/2 w-96 h-96 bg-gradient-to-br from-indigo-200/30 to-blue-200/30 rounded-full blur-3xl animate-blob animation-delay-4000"
    >
    </div>
  </div>

  <!-- Content -->
  <div
    class="w-full relative z-10 py-16 px-4 sm:py-20 sm:px-8 lg:px-16 xl:px-24"
  >
    <div
      class="flex flex-col gap-8 sm:gap-12 lg:gap-16 items-center justify-center"
    >
      <!-- Hero content -->
      <div class="text-center space-y-4 sm:space-y-6 max-w-6xl mx-auto w-full">
        <!-- Welcome text -->
        <div class="space-y-2">
          <span
            class="block text-xs sm:text-sm font-semibold tracking-[0.15em] sm:tracking-[0.2em] uppercase text-primary/70"
          >
            Impact Developer Programm
          </span>
        </div>

        <!-- Main title with animated gradient -->
        <h1 class="hero-title relative px-2">
          <span
            class="block text-3xl xs:text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-black tracking-tight leading-tight"
          >
            <span
              class="bg-gradient-to-r from-primary via-secondary to-tertiary bg-clip-text text-transparent animate-gradient-x break-words"
            >
              Von Developer zum<br class="hidden sm:block" />Tech Lead
            </span>
          </span>
        </h1>

        <!-- Subtitle -->
        <p
          class="text-base xs:text-lg sm:text-xl md:text-2xl text-gray-700 max-w-3xl mx-auto leading-relaxed font-light px-4 sm:px-0"
        >
          Entwickle dich zum technischen Leader, der sowohl fachlich als auch menschlich überzeugt. Ein maßgeschneidertes 1:1 Software Coaching Format.
        </p>

        <!-- CTA Buttons -->
        <div
          class="flex flex-col sm:flex-row gap-4 justify-center items-center pt-4 sm:pt-6"
        >
          <a
            href="#transformation"
            class="group relative inline-flex items-center gap-2 px-6 sm:px-8 md:px-10 py-3 sm:py-4 bg-gradient-to-r from-primary to-secondary text-white font-semibold rounded-full shadow-xl hover:shadow-2xl transform hover:-translate-y-1 transition-all duration-300 text-sm sm:text-base"
            aria-label="Mehr zum Impact Developer Programm erfahren"
          >
            <span>Mehr erfahren</span>
            <svg
              class="w-4 h-4 sm:w-5 sm:h-5 group-hover:translate-x-1 transition-transform"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
            </svg>
            <!-- Shine effect -->
            <div
              class="absolute inset-0 rounded-full bg-gradient-to-r from-transparent via-white/20 to-transparent -translate-x-full group-hover:translate-x-full transition-transform duration-700"
            >
            </div>
          </a>

          <a
            href="https://calendly.com/gregor-entwicklungszeit/30min"
            target="_blank"
            rel="noopener noreferrer"
            class="group relative inline-flex items-center gap-2 px-6 sm:px-8 md:px-10 py-3 sm:py-4 bg-white/80 backdrop-blur-sm text-primary font-semibold rounded-full shadow-lg hover:shadow-xl border-2 border-primary/20 hover:border-primary/40 transform hover:-translate-y-1 transition-all duration-300 text-sm sm:text-base"
            aria-label="Termin buchen (öffnet neues Fenster)"
          >
            <span>Termin buchen</span>
            <svg
              class="w-4 h-4 sm:w-5 sm:h-5 group-hover:translate-x-1 transition-transform"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
              ></path>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Hero section animations */
  .coaching-hero-section {
    animation: fadeIn 1s ease-out;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  /* Animated blob backgrounds */
  @keyframes blob {
    0% {
      transform: translate(0px, 0px) scale(1);
    }
    33% {
      transform: translate(30px, -50px) scale(1.1);
    }
    66% {
      transform: translate(-20px, 20px) scale(0.9);
    }
    100% {
      transform: translate(0px, 0px) scale(1);
    }
  }

  .animate-blob {
    animation: blob 7s infinite;
  }

  .animation-delay-2000 {
    animation-delay: 2s;
  }

  .animation-delay-4000 {
    animation-delay: 4s;
  }

  /* Gradient text animation */
  @keyframes gradient-x {
    0%,
    100% {
      background-size: 200% 200%;
      background-position: left center;
    }
    50% {
      background-size: 200% 200%;
      background-position: right center;
    }
  }

  .animate-gradient-x {
    animation: gradient-x 4s ease infinite;
  }

  /* Hero title animation */
  .hero-title {
    animation: slideUp 1s ease-out 0.2s both;
  }

  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Mobile-specific adjustments */
  @media (max-width: 640px) {
    .coaching-hero-section {
      min-height: 100vh;
      min-height: 100dvh;
    }

    .animate-blob {
      width: 200px;
      height: 200px;
    }
  }

  /* Backdrop blur enhancement */
  .backdrop-blur-sm {
    backdrop-filter: blur(4px);
  }

  /* Smooth transitions for interactive elements */
  a,
  button {
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }
</style>
