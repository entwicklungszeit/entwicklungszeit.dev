---

---

<section
  class="konflikt-hero-section relative flex items-center overflow-visible"
>
  <!-- Gradient background with soft blobs -->
  <div class="absolute inset-0">
    <div
      class="absolute inset-0 bg-gradient-to-br from-blue-50/50 via-purple-50/30 to-pink-50/50"
    >
    </div>
    <div
      class="absolute top-0 -left-40 w-96 h-96 bg-gradient-to-br from-blue-200/20 to-indigo-200/20 rounded-full blur-3xl"
    >
    </div>
    <div
      class="absolute top-40 -right-40 w-64 h-64 bg-gradient-to-br from-purple-200/20 to-pink-200/20 rounded-full blur-3xl"
    >
    </div>
  </div>

  <!-- Content -->
  <div
    class="relative z-10 w-full py-16 px-4 sm:py-20 sm:px-8 lg:px-16 xl:px-24"
  >
    <div class="max-w-4xl mx-auto text-center space-y-6 sm:space-y-8">
      <!-- Eyebrow -->
      <span
        class="block text-xs sm:text-sm font-semibold tracking-[0.15em] sm:tracking-[0.2em] uppercase text-primary/70"
      >
        Für 0€ - Dein E-Mail-Kurs zur Konfliktkommunikation
      </span>

      <!-- Main title -->
      <h1 class="relative px-2">
        <span
          class="block text-3xl xs:text-4xl sm:text-5xl md:text-6xl font-black tracking-tight leading-tight"
        >
          <span
            class="bg-gradient-to-r from-primary via-secondary to-tertiary bg-clip-text text-transparent break-words"
          >
            Konfliktkommunikation <br class="hidden sm:block" /> klar bleiben, wenn
            es schwierig wird
          </span>
        </span>
        <span
          class="absolute -bottom-3 left-1/2 -translate-x-1/2 w-1/3 h-1.5 bg-gradient-to-r from-primary via-secondary to-tertiary rounded-full"
        ></span>
      </h1>

      <!-- Subtitle -->
      <p
        class="text-base xs:text-lg sm:text-xl text-gray-700 max-w-2xl mx-auto leading-relaxed"
      >
        Lerne in 6 Wochen, Konflikte konstruktiv zu klären – mit praktischen
        Lektionen und Übungen direkt in dein Postfach.
      </p>

      <!-- CTA scroll to form -->
      <a
        href="#anmeldung"
        class="group inline-flex items-center gap-2 px-6 sm:px-8 py-3 sm:py-4 bg-gradient-to-r from-primary to-secondary text-white font-semibold rounded-full shadow-xl hover:shadow-2xl transform hover:-translate-y-1 transition-all duration-300 text-sm sm:text-base"
        aria-label="Zum Anmeldeformular scrollen"
      >
        <span>Kostenlos anmelden</span>
        <svg
          class="w-4 h-4 sm:w-5 sm:h-5 group-hover:translate-y-1 transition-transform"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
      </a>
    </div>
  </div>
</section>
